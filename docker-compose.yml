services:
  redis:
    image: redis:7-alpine
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD}"]
    ports:
      - "6379:6379"
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    env_file:
      - .env.docker
    restart: always

  transcripter:
    image: ${MQ_IMAGE:-reclara-mq:latest}
    build:
      context: .
      dockerfile: ./apps/mq/Dockerfile
      tags:
        - "reclara-mq:latest"
    working_dir: /app/apps/mq
    command: ["bun", "run", "start:transcripter"]
    depends_on:
      - redis
    env_file:
      - .env.docker
    restart: always

  summarizer:
    image: ${MQ_IMAGE:-reclara-mq:latest}
    build:
      context: .
      dockerfile: ./apps/mq/Dockerfile
      tags:
        - "reclara-mq:latest"
    working_dir: /app/apps/mq
    command: ["bun", "run", "start:summarizer"]
    depends_on:
      - redis
    env_file:
      - .env.docker
    restart: always
